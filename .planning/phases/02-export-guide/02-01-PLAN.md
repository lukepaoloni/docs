---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "export-guide.mdx"
autonomous: true
requirements:
  - "EXPORT-01"
  - "EXPORT-02"
must_haves:
  truths:
    - "A merchant can open /export-guide and follow each numbered step to completion without external resources"
    - "Every step has an expected result in plain language"
    - "The page ends with a 'What to do next' card group with at least three paths"
  artifacts:
    - path: "export-guide.mdx"
      provides: "Export walkthrough page with numbered steps and action cards"
      min_lines: 50
      exports: "export-guide"
  key_links:
    - from: "export-guide.mdx"
      to: "csv-reference.mdx"
      via: "cross-link for 'edit and re-import' path"
      pattern: "csv-reference"
    - from: "docs.json"
      to: "export-guide.mdx"
      via: "navigation registration"
      pattern: "\"export-guide\""
---

<objective>
Write the complete Export Guide page so merchants can download their current rate table as a CSV by following a numbered walkthrough, ending with actionable next steps.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs.json
@export-guide.mdx
@index.mdx

# Content principles from CLAUDE.md (MANDATORY)
- Audience: non-technical merchants — plain language, numbered steps, screenshots
- Action-Result framing: every step states what the merchant does AND what they'll see next
- Error-Cause-Fix for troubleshooting; Action-Result for guides
- Use Mintlify components: `<Steps>`, `<Frame>`, `<Warning>`, `<Note>`, `<Tip>`, `<Card>`, `<CardGroup>`
- No technical jargon: avoid "parse", "delimiter", "schema"; use "column", "file", "value"

# Phase 1 decisions (LOCKED — maintain consistency)
- D-03: URL slugs are kebab-case and LOCKED. Export Guide path = `/export-guide`.
- Single "Guides" tab with all sections. All pages under one tab.
</context>

<interfaces>
<!-- Key Mintlify components for this phase -->

From Mintlify docs (known patterns):
```mdx
<Steps>
  <step>
    ## Step title
    Step body with action description.
  </step>
</Steps>

<CardGroup>
  <Card title="Card title" icon="icon-name">
    Card body text.
  </Card>
</CardGroup>

<Note>
  Note text here.
</Note>

<Frame caption="Screenshot description">
  ![Alt text](/path/to/screenshot.png)
</Frame>
```

Navigation registered in docs.json: index, export-guide, csv-reference, import-guide, troubleshooting (from Phase 1).
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Write numbered export walkthrough</name>
  <files>export-guide.mdx</files>
  <read_first>
    - export-guide.mdx (current placeholder content — replace, do not append)
    - docs.json (confirm navigation path is registered)
    - CLAUDE.md content principles
  </read_first>
  <action>
Replace the entire `export-guide.mdx` frontmatter and content with a complete, production-ready export walkthrough page. The page must:

1. **Frontmatter** (keep existing):
   ```yaml
   ---
   title: "Export Guide"
   description: "How to download your shipping rate table as a CSV file."
   ---
   ```

2. **Intro paragraph** — 1-2 sentences explaining what the merchant will do in this guide. Plain language, no jargon.

3. **Numbered walkthrough using `<Steps>`** — Write 4-6 steps that cover the complete export flow. Based on what MatrixRates Import/Export likely does:
   - Step 1: Open the MatrixRates app from Shopify admin
   - Step 2: Find the export option (look for "Export" button or menu)
   - Step 3: Choose CSV format if prompted
   - Step 4: Click the download/export button
   - Step 5: Find the downloaded file on your computer

   **For each step:**
   - State the action in plain language (e.g., "In the MatrixRates app, click the Export button")
   - State the expected result immediately after (e.g., "Your browser will download a file called rates.csv")

   **Use `<Note>` or `<Tip>` blocks** to highlight where the file saves (Downloads folder by default) and what to do if the file doesn't appear.

4. **Use `<Frame>` with `![empty](/path/placeholder.png)`** — Add a placeholder for screenshots. Include a caption describing what the merchant should see at that step. Format: `![Description of screenshot](/images/export-step-X.png)` where X is the step number.

5. **DO NOT use technical jargon** — Write for a non-technical merchant. Test every sentence: could a person who has never used a CSV file understand it?

6. **DO NOT include a troubleshooting section** — that belongs to Phase 5 (Troubleshooting Guide). Link to `troubleshooting.mdx` only if a natural "what if it doesn't work" moment exists, but keep it brief.

7. **End of walkthrough section** — Add a `<Note>` confirming the export is complete and pointing to the next section.
</action>
  <verify>
    <automated>
      grep -c "## Step" export-guide.mdx && \
      grep -c "<Steps>" export-guide.mdx && \
      grep -c "</Steps>" export-guide.mdx && \
      grep -c "rates.csv" export-guide.mdx && \
      grep -c "export-guide" .planning/ROADMAP.md
    </automated>
  </verify>
  <done>
    export-guide.mdx contains a complete numbered walkthrough (4-6 steps) with `<Steps>` component, each step has both an action and an expected result. No placeholder text ("coming soon") remains. File is 50+ lines of actual content.
  </done>
</task>

<task type="auto">
  <name>task 2: Add What to do next cards</name>
  <files>export-guide.mdx</files>
  <read_first>
    - export-guide.mdx (after task 1 completes)
  </read_first>
  <action>
After the walkthrough section in `export-guide.mdx`, add a `<CardGroup>` with at least three actionable cards. These represent the paths a merchant takes after exporting their rate table:

```mdx
<CardGroup>
  <Card title="Edit and re-import" icon="edit-3">
    Open the CSV in a spreadsheet app, update your rates, then follow the [Import Guide](/import-guide) to upload the changes back to MatrixRates.
  </Card>
  <Card title="Keep as a backup" icon="folder">
    Save the exported file in a safe location — like a dedicated folder for your store settings. You can use it to restore rates if anything goes wrong.
  </Card>
  <Card title="Share with your team" icon="users">
    Email the CSV file to teammates or upload it to your internal documentation so everyone has the same up-to-date rates.
  </Card>
</CardGroup>
```

**Rules:**
- Each `<Card>` must have: `title`, and a brief body that explains WHAT to do and WHY it matters (from the merchant's perspective).
- Icon names must be valid Lucide icon names (e.g., `edit-3`, `folder`, `users`, `download`, `file-text`, `mail`, `shield`).
- Body text must be 1-2 sentences. Plain language only.
- Cards must link to real pages registered in docs.json: `export-guide`, `import-guide`, `csv-reference`, `troubleshooting`.
- The "Edit and re-import" card MUST cross-link to `import-guide.mdx` using the path `/import-guide`.
- Do NOT link to pages that don't exist yet (`csv-reference` is ok since Phase 1 created the stub).
- If adding a fourth card (optional), consider: "Learn about the CSV format" linking to `/csv-reference`.
</action>
  <verify>
    <automated>
      grep -c "<CardGroup>" export-guide.mdx && \
      grep -c "<Card" export-guide.mdx && \
      grep -c "import-guide" export-guide.mdx
    </automated>
  </verify>
  <done>
    export-guide.mdx ends with a CardGroup containing 3+ cards. Each card has a title and actionable body text. "Edit and re-import" card links to /import-guide. No placeholder card text ("Card content here") remains.
  </done>
</task>

</tasks>

<verification>
## Overall phase checks

1. `export-guide.mdx` exists and is not the placeholder stub
2. File contains `<Steps>` component with 4-6 steps
3. File contains `<CardGroup>` with at least 3 cards
4. At least one card links to `/import-guide`
5. No remaining "coming soon" or placeholder text
6. docs.json navigation includes `export-guide` (confirmed from Phase 1)
</verification>

<success_criteria>
- Merchant can open /export-guide and follow steps 1-6 without external resources
- Every step has an expected result stated in plain language
- Page ends with 3+ actionable "What to do next" cards
- EXPORT-01 (numbered walkthrough) and EXPORT-02 (what to do next cards) both satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-export-guide/02-01-SUMMARY.md`
</output>