---
phase: "01"
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - favicon.png
  - logo/light.svg
  - logo/dark.svg
autonomous: false
requirements:
  - SITE-03
user_setup:
  - service: brand_assets
    why: "Required for site rebranding"
    env_vars: []
    dashboard_config:
      - task: "Provide favicon.png"
        location: "Place favicon.png in repository root"
      - task: "Replace logo files"
        location: "Replace logo/light.svg and logo/dark.svg with MatrixRates brand logos"
must_haves:
  truths:
    - "favicon.png exists in repository root and is referenced by docs.json"
    - "logo/light.svg and logo/dark.svg are MatrixRates brand assets"
  artifacts:
    - path: favicon.png
      provides: Site favicon displayed in browser tab
    - path: logo/light.svg
      provides: Light-mode logo in navbar
    - path: logo/dark.svg
      provides: Dark-mode logo in navbar
  key_links:
    - from: docs.json
      to: favicon.png
      via: favicon field
      pattern: "/favicon.png"
---

<objective>
User provides brand assets: favicon.png and logo files. This is a human action required before the site can display MatrixRates branding.
</objective>

<context>
@docs.json
@.planning/phases/01-site-rebrand-scaffold/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Provide brand assets (favicon and logos)</name>
  <files>favicon.png, logo/light.svg, logo/dark.svg</files>
  <action>
User must complete these actions:

1. **favicon.png**: Place a PNG favicon file in the repository root. The file must be named exactly `favicon.png`. This will be served as the browser tab icon.

2. **logo/light.svg**: Replace `logo/light.svg` with the MatrixRates light-mode logo. This appears in the navbar when the user's system is in light mode.

3. **logo/dark.svg**: Replace `logo/dark.svg` with the MatrixRates dark-mode logo. This appears in the navbar when the user's system is in dark mode.

The docs.json configuration already points to these paths:
- favicon: "/favicon.png"
- logo: "/logo/light.svg" and "/logo/dark.svg"

No changes to docs.json are needed for the logo paths - they already point to the correct locations.
</action>
  <verify>
<automated>ls -la favicon.png logo/light.svg logo/dark.svg 2>&1</automated>
  </verify>
  <done>
All three brand asset files exist in the correct locations:
- favicon.png in repository root
- logo/light.svg replaced with brand asset
- logo/dark.svg replaced with brand asset
</done>
</task>

</tasks>

<verification>
- favicon.png exists at repository root
- logo/light.svg is a valid SVG file (not the Mintlify default)
- logo/dark.svg is a valid SVG file (not the Mintlify default)
</verification>

<success_criteria>
- [x] Favicon displays in browser tab
- [x] Light logo displays in navbar (light mode)
- [x] Dark logo displays in navbar (dark mode)
- [x] No Mintlify leaf icon visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-rebrand-scaffold/01-03-SUMMARY.md`
</output>
