---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs.json
autonomous: true
requirements:
  - SITE-01
  - SITE-04
user_setup: []
must_haves:
  truths:
    - "docs.json contains MatrixRates name, teal brand color, and locked navigation structure"
    - "Support email (support@dojoapps.co.uk) appears in navbar"
    - "All 5 page paths are registered in docs.json before any content exists"
  artifacts:
    - path: docs.json
      provides: Complete site configuration
      contains: name, colors, navigation, navbar
  key_links:
    - from: docs.json
      to: Navigation structure
      via: navigation.tabs[].groups[].pages[]
      pattern: index, export-guide, csv-reference, import-guide, troubleshooting
---

<objective>
Update docs.json with MatrixRates branding, colors, navigation structure, and site configuration. This is the foundational configuration that all future phases depend on.
</objective>

<context>
@docs.json
@.planning/phases/01-site-rebrand-scaffold/01-CONTEXT.md
@.planning/phases/01-site-rebrand-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update docs.json with MatrixRates branding and navigation</name>
  <files>docs.json</files>
  <action>
Update the following fields in docs.json:

1. **name**: Change to "MatrixRates Import/Export"
2. **colors.primary**: Set to "#2dd4bf" (teal)
3. **colors.light**: Set to "#5eead4" (lighter teal for dark mode)
4. **colors.dark**: Set to "#0d9488" (darker teal for buttons)
5. **favicon**: Change from "/favicon.svg" to "/favicon.png"
6. **theme**: Keep as "mint" (Claude's Discretion)

**Navigation Structure:**
- Replace navigation.tabs with single "Guides" tab
- Single group "MatrixRates Import/Export" with pages: index, export-guide, csv-reference, import-guide, troubleshooting
- Remove navigation.global block entirely

**Navbar:**
- Replace existing links with single Support link: label "Support", href "mailto:support@dojoapps.co.uk"
- Remove "primary" key entirely (no CTA button)

**Footer:**
- Remove "footer" key entirely

**Contextual Options:**
- Keep only: ["copy", "view", "chatgpt", "claude", "perplexity"]
</action>
  <verify>
<automated>grep -E '"name":|"primary":|"favicon":|"tab":|"Support"' docs.json</automated>
  </verify>
  <done>
docs.json contains: "MatrixRates Import/Export" name, #2dd4bf primary color, /favicon.png, Support mailto link, single "Guides" tab with all 5 pages registered
</done>
</task>

</tasks>

<verification>
- docs.json is valid JSON (parse check)
- Navigation structure has all 5 pages registered
- No "Mint Starter Kit" text remains in any string field
- Support email is correctly formatted as mailto link
</verification>

<success_criteria>
- [x] docs.json updated with MatrixRates branding
- [x] Navigation structure locked with all 5 page paths
- [x] Support email in navbar
- [x] No API Reference tab in navigation
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-rebrand-scaffold/01-01-SUMMARY.md`
</output>
